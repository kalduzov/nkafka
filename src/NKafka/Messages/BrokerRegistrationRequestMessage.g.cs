//  This is an independent project of an individual developer. Dear PVS-Studio, please check it.
// 
//  PVS-Studio Static Code Analyzer for C, C++, C#, and Java: https://pvs-studio.com
// 
//  Copyright ©  2022 Aleksey Kalduzov. All rights reserved
// 
//  Author: Aleksey Kalduzov
//  Email: alexei.kalduzov@gmail.com
// 
//  Licensed under the Apache License, Version 2.0 (the "License");
//  you may not use this file except in compliance with the License.
//  You may obtain a copy of the License at
// 
//      https://www.apache.org/licenses/LICENSE-2.0
// 
//  Unless required by applicable law or agreed to in writing, software
//  distributed under the License is distributed on an "AS IS" BASIS,
//  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
//  See the License for the specific language governing permissions and
//  limitations under the License.
//
// <auto-generated> THIS CODE IS AUTOMATICALLY GENERATED.  DO NOT EDIT. </auto-generated>

// ReSharper disable RedundantUsingDirective
// ReSharper disable UnusedType.Global
// ReSharper disable UnusedMember.Global
// ReSharper disable ClassNeverInstantiated.Global
// ReSharper disable PartialTypeWithSinglePart

#nullable enable

using NKafka.Exceptions;
using NKafka.Protocol;
using NKafka.Protocol.Extensions;
using NKafka.Protocol.Records;
using System.Text;

namespace NKafka.Messages;

public sealed partial class BrokerRegistrationRequestMessage: IRequestMessage, IEquatable<BrokerRegistrationRequestMessage>
{
    /// <inheritdoc />
    public ApiKeys ApiKey => ApiKeys.BrokerRegistration;

    public const bool ONLY_CONTROLLER = true;

    /// <inheritdoc />
    public bool OnlyController => ONLY_CONTROLLER;

    public List<TaggedField>? UnknownTaggedFields { get; set; } = null;

    /// <summary>
    /// The broker ID.
    /// </summary>
    public int BrokerId { get; set; } = 0;

    /// <summary>
    /// The cluster id of the broker process.
    /// </summary>
    public string ClusterId { get; set; } = string.Empty;

    /// <summary>
    /// The incarnation id of the broker process.
    /// </summary>
    public Guid IncarnationId { get; set; } = Guid.Empty;

    /// <summary>
    /// The listeners of this broker
    /// </summary>
    public ListenerCollection Listeners { get; set; } = new ();

    /// <summary>
    /// The features on this broker
    /// </summary>
    public FeatureCollection Features { get; set; } = new ();

    /// <summary>
    /// The rack which this broker is in.
    /// </summary>
    public string Rack { get; set; } = string.Empty;

    /// <summary>
    /// If the required configurations for ZK migration are present, this value is set to true
    /// </summary>
    public bool IsMigratingZkBroker { get; set; } = false;

    public BrokerRegistrationRequestMessage()
    {
    }

    public BrokerRegistrationRequestMessage(BufferReader reader, ApiVersion version)
        : this()
    {
        Read(reader, version);
    }

    public void Read(BufferReader reader, ApiVersion version)
    {
        BrokerId = reader.ReadInt();
        {
            int length;
            length = reader.ReadVarUInt() - 1;
            if (length < 0)
            {
                throw new Exception("non-nullable field ClusterId was serialized as null");
            }
            else if (length > 0x7fff)
            {
                throw new Exception($"string field ClusterId had invalid length {length}");
            }
            else
            {
                ClusterId = reader.ReadString(length);
            }
        }
        IncarnationId = reader.ReadGuid();
        {
            int arrayLength;
            arrayLength = reader.ReadVarUInt() - 1;
            if (arrayLength < 0)
            {
                throw new Exception("non-nullable field Listeners was serialized as null");
            }
            else
            {
                var newCollection = new ListenerCollection(arrayLength);
                for (var i = 0; i < arrayLength; i++)
                {
                    newCollection.Add(new ListenerMessage(reader, version));
                }
                Listeners = newCollection;
            }
        }
        {
            int arrayLength;
            arrayLength = reader.ReadVarUInt() - 1;
            if (arrayLength < 0)
            {
                throw new Exception("non-nullable field Features was serialized as null");
            }
            else
            {
                var newCollection = new FeatureCollection(arrayLength);
                for (var i = 0; i < arrayLength; i++)
                {
                    newCollection.Add(new FeatureMessage(reader, version));
                }
                Features = newCollection;
            }
        }
        {
            int length;
            length = reader.ReadVarUInt() - 1;
            if (length < 0)
            {
                Rack = null;
            }
            else if (length > 0x7fff)
            {
                throw new Exception($"string field Rack had invalid length {length}");
            }
            else
            {
                Rack = reader.ReadString(length);
            }
        }
        if (version >= ApiVersion.Version1)
        {
            IsMigratingZkBroker = reader.ReadByte() != 0;
        }
        else
        {
            IsMigratingZkBroker = false;
        }
        UnknownTaggedFields = null;
        var numTaggedFields = reader.ReadVarUInt();
        for (var t = 0; t < numTaggedFields; t++)
        {
            var tag = reader.ReadVarUInt();
            var size = reader.ReadVarUInt();
            switch (tag)
            {
                default:
                    UnknownTaggedFields = reader.ReadUnknownTaggedField(UnknownTaggedFields, tag, size);
                    break;
            }
        }
    }

    public void Write(BufferWriter writer, ApiVersion version)
    {
        var numTaggedFields = 0;
        writer.WriteInt(BrokerId);
        {
            var stringBytes = Encoding.UTF8.GetBytes(ClusterId);
            writer.WriteVarUInt(stringBytes.Length + 1);
            writer.WriteBytes(stringBytes);
        }
        writer.WriteGuid(IncarnationId);
        writer.WriteVarUInt(Listeners.Count + 1);
        foreach (var element in Listeners)
        {
            element.Write(writer, version);
        }
        writer.WriteVarUInt(Features.Count + 1);
        foreach (var element in Features)
        {
            element.Write(writer, version);
        }
        if (Rack is null)
        {
            writer.WriteVarUInt(0);
        }
        else
        {
            var stringBytes = Encoding.UTF8.GetBytes(Rack);
            writer.WriteVarUInt(stringBytes.Length + 1);
            writer.WriteBytes(stringBytes);
        }
        if (version >= ApiVersion.Version1)
        {
            writer.WriteBool(IsMigratingZkBroker);
        }
        else
        {
            if (IsMigratingZkBroker)
            {
                throw new UnsupportedVersionException($"Attempted to write a non-default IsMigratingZkBroker at version {version}");
            }
        }
        var rawWriter = RawTaggedFieldWriter.ForFields(UnknownTaggedFields);
        numTaggedFields += rawWriter.FieldsCount;
        writer.WriteVarUInt(numTaggedFields);
        rawWriter.WriteRawTags(writer, int.MaxValue);
    }

    public override bool Equals(object? obj)
    {
        return ReferenceEquals(this, obj) || obj is BrokerRegistrationRequestMessage other && Equals(other);
    }

    public bool Equals(BrokerRegistrationRequestMessage? other)
    {
        if (other is null)
        {
            return false;
        }
        if (BrokerId != other.BrokerId)
        {
            return false;
        }
        if (ClusterId is null)
        {
            if (other.ClusterId is not null)
            {
                return false;
            }
        }
        else
        {
            if (!ClusterId.Equals(other.ClusterId))
            {
                return false;
            }
        }
        if (!IncarnationId.Equals(other.IncarnationId))
        {
            return false;
        }
        if (Listeners is null)
        {
            if (other.Listeners is not null)
            {
                return false;
            }
        }
        else
        {
            if (!Listeners.SequenceEqual(other.Listeners))
            {
                return false;
            }
        }
        if (Features is null)
        {
            if (other.Features is not null)
            {
                return false;
            }
        }
        else
        {
            if (!Features.SequenceEqual(other.Features))
            {
                return false;
            }
        }
        if (Rack is null)
        {
            if (other.Rack is not null)
            {
                return false;
            }
        }
        else
        {
            if (!Rack.Equals(other.Rack))
            {
                return false;
            }
        }
        if (IsMigratingZkBroker != other.IsMigratingZkBroker)
        {
            return false;
        }
        return UnknownTaggedFields.CompareRawTaggedFields(other.UnknownTaggedFields);
    }

    public override int GetHashCode()
    {
        var hashCode = 0;
        hashCode = HashCode.Combine(hashCode, BrokerId, ClusterId, IncarnationId, Listeners, Features, Rack, IsMigratingZkBroker);
        return hashCode;
    }

    public override string ToString()
    {
        return "BrokerRegistrationRequestMessage("
            + "BrokerId=" + BrokerId
            + ", ClusterId=" + (string.IsNullOrWhiteSpace(ClusterId) ? "null" : ClusterId)
            + ", IncarnationId=" + IncarnationId
            + ", Listeners=" + Listeners.DeepToString()
            + ", Features=" + Features.DeepToString()
            + ", Rack=" + (string.IsNullOrWhiteSpace(Rack) ? "null" : Rack)
            + ", IsMigratingZkBroker=" + (IsMigratingZkBroker ? "true" : "false")
            + ")";
    }

    public sealed partial class ListenerMessage: IMessage, IEquatable<ListenerMessage>
    {
        public List<TaggedField>? UnknownTaggedFields { get; set; } = null;

        /// <summary>
        /// The name of the endpoint.
        /// </summary>
        public string Name { get; set; } = string.Empty;

        /// <summary>
        /// The hostname.
        /// </summary>
        public string Host { get; set; } = string.Empty;

        /// <summary>
        /// The port.
        /// </summary>
        public ushort Port { get; set; } = 0;

        /// <summary>
        /// The security protocol.
        /// </summary>
        public short SecurityProtocol { get; set; } = 0;

        public ListenerMessage()
        {
        }

        public ListenerMessage(BufferReader reader, ApiVersion version)
            : this()
        {
            Read(reader, version);
        }

        public void Read(BufferReader reader, ApiVersion version)
        {
            if (version > ApiVersion.Version1)
            {
                throw new UnsupportedVersionException($"Can't read version {version} of ListenerMessage");
            }
            {
                int length;
                length = reader.ReadVarUInt() - 1;
                if (length < 0)
                {
                    throw new Exception("non-nullable field Name was serialized as null");
                }
                else if (length > 0x7fff)
                {
                    throw new Exception($"string field Name had invalid length {length}");
                }
                else
                {
                    Name = reader.ReadString(length);
                }
            }
            {
                int length;
                length = reader.ReadVarUInt() - 1;
                if (length < 0)
                {
                    throw new Exception("non-nullable field Host was serialized as null");
                }
                else if (length > 0x7fff)
                {
                    throw new Exception($"string field Host had invalid length {length}");
                }
                else
                {
                    Host = reader.ReadString(length);
                }
            }
            Port = reader.ReadUShort();
            SecurityProtocol = reader.ReadShort();
            UnknownTaggedFields = null;
            var numTaggedFields = reader.ReadVarUInt();
            for (var t = 0; t < numTaggedFields; t++)
            {
                var tag = reader.ReadVarUInt();
                var size = reader.ReadVarUInt();
                switch (tag)
                {
                    default:
                        UnknownTaggedFields = reader.ReadUnknownTaggedField(UnknownTaggedFields, tag, size);
                        break;
                }
            }
        }

        public void Write(BufferWriter writer, ApiVersion version)
        {
            var numTaggedFields = 0;
            {
                var stringBytes = Encoding.UTF8.GetBytes(Name);
                writer.WriteVarUInt(stringBytes.Length + 1);
                writer.WriteBytes(stringBytes);
            }
            {
                var stringBytes = Encoding.UTF8.GetBytes(Host);
                writer.WriteVarUInt(stringBytes.Length + 1);
                writer.WriteBytes(stringBytes);
            }
            writer.WriteUShort(Port);
            writer.WriteShort(SecurityProtocol);
            var rawWriter = RawTaggedFieldWriter.ForFields(UnknownTaggedFields);
            numTaggedFields += rawWriter.FieldsCount;
            writer.WriteVarUInt(numTaggedFields);
            rawWriter.WriteRawTags(writer, int.MaxValue);
        }

        public override bool Equals(object? obj)
        {
            return ReferenceEquals(this, obj) || obj is ListenerMessage other && Equals(other);
        }

        public bool Equals(ListenerMessage? other)
        {
            if (other is null)
            {
                return false;
            }
            if (Name is null)
            {
                if (other.Name is not null)
                {
                    return false;
                }
            }
            else
            {
                if (!Name.Equals(other.Name))
                {
                    return false;
                }
            }
            if (Host is null)
            {
                if (other.Host is not null)
                {
                    return false;
                }
            }
            else
            {
                if (!Host.Equals(other.Host))
                {
                    return false;
                }
            }
            if (Port != other.Port)
            {
                return false;
            }
            if (SecurityProtocol != other.SecurityProtocol)
            {
                return false;
            }
            return UnknownTaggedFields.CompareRawTaggedFields(other.UnknownTaggedFields);
        }

        public override int GetHashCode()
        {
            var hashCode = 0;
            hashCode = HashCode.Combine(hashCode, Name);
            return hashCode;
        }

        public override string ToString()
        {
            return "ListenerMessage("
                + "Name=" + (string.IsNullOrWhiteSpace(Name) ? "null" : Name)
                + ", Host=" + (string.IsNullOrWhiteSpace(Host) ? "null" : Host)
                + ", Port=" + Port
                + ", SecurityProtocol=" + SecurityProtocol
                + ")";
        }
    }

    public sealed partial class ListenerCollection: HashSet<ListenerMessage>
    {
        public ListenerCollection()
        {
        }

        public ListenerCollection(int capacity)
            : base(capacity)
        {
        }
        public override bool Equals(object? obj)
        {
            return SetEquals((IEnumerable<ListenerMessage>)obj);
        }
    }

    public sealed partial class FeatureMessage: IMessage, IEquatable<FeatureMessage>
    {
        public List<TaggedField>? UnknownTaggedFields { get; set; } = null;

        /// <summary>
        /// The feature name.
        /// </summary>
        public string Name { get; set; } = string.Empty;

        /// <summary>
        /// The minimum supported feature level.
        /// </summary>
        public short MinSupportedVersion { get; set; } = 0;

        /// <summary>
        /// The maximum supported feature level.
        /// </summary>
        public short MaxSupportedVersion { get; set; } = 0;

        public FeatureMessage()
        {
        }

        public FeatureMessage(BufferReader reader, ApiVersion version)
            : this()
        {
            Read(reader, version);
        }

        public void Read(BufferReader reader, ApiVersion version)
        {
            if (version > ApiVersion.Version1)
            {
                throw new UnsupportedVersionException($"Can't read version {version} of FeatureMessage");
            }
            {
                int length;
                length = reader.ReadVarUInt() - 1;
                if (length < 0)
                {
                    throw new Exception("non-nullable field Name was serialized as null");
                }
                else if (length > 0x7fff)
                {
                    throw new Exception($"string field Name had invalid length {length}");
                }
                else
                {
                    Name = reader.ReadString(length);
                }
            }
            MinSupportedVersion = reader.ReadShort();
            MaxSupportedVersion = reader.ReadShort();
            UnknownTaggedFields = null;
            var numTaggedFields = reader.ReadVarUInt();
            for (var t = 0; t < numTaggedFields; t++)
            {
                var tag = reader.ReadVarUInt();
                var size = reader.ReadVarUInt();
                switch (tag)
                {
                    default:
                        UnknownTaggedFields = reader.ReadUnknownTaggedField(UnknownTaggedFields, tag, size);
                        break;
                }
            }
        }

        public void Write(BufferWriter writer, ApiVersion version)
        {
            var numTaggedFields = 0;
            {
                var stringBytes = Encoding.UTF8.GetBytes(Name);
                writer.WriteVarUInt(stringBytes.Length + 1);
                writer.WriteBytes(stringBytes);
            }
            writer.WriteShort(MinSupportedVersion);
            writer.WriteShort(MaxSupportedVersion);
            var rawWriter = RawTaggedFieldWriter.ForFields(UnknownTaggedFields);
            numTaggedFields += rawWriter.FieldsCount;
            writer.WriteVarUInt(numTaggedFields);
            rawWriter.WriteRawTags(writer, int.MaxValue);
        }

        public override bool Equals(object? obj)
        {
            return ReferenceEquals(this, obj) || obj is FeatureMessage other && Equals(other);
        }

        public bool Equals(FeatureMessage? other)
        {
            if (other is null)
            {
                return false;
            }
            if (Name is null)
            {
                if (other.Name is not null)
                {
                    return false;
                }
            }
            else
            {
                if (!Name.Equals(other.Name))
                {
                    return false;
                }
            }
            if (MinSupportedVersion != other.MinSupportedVersion)
            {
                return false;
            }
            if (MaxSupportedVersion != other.MaxSupportedVersion)
            {
                return false;
            }
            return UnknownTaggedFields.CompareRawTaggedFields(other.UnknownTaggedFields);
        }

        public override int GetHashCode()
        {
            var hashCode = 0;
            hashCode = HashCode.Combine(hashCode, Name);
            return hashCode;
        }

        public override string ToString()
        {
            return "FeatureMessage("
                + "Name=" + (string.IsNullOrWhiteSpace(Name) ? "null" : Name)
                + ", MinSupportedVersion=" + MinSupportedVersion
                + ", MaxSupportedVersion=" + MaxSupportedVersion
                + ")";
        }
    }

    public sealed partial class FeatureCollection: HashSet<FeatureMessage>
    {
        public FeatureCollection()
        {
        }

        public FeatureCollection(int capacity)
            : base(capacity)
        {
        }
        public override bool Equals(object? obj)
        {
            return SetEquals((IEnumerable<FeatureMessage>)obj);
        }
    }
}
