//44-A6-3A-D2-34-0A-C6-47-D0-56-EA-82-F6-63-F2-C2-A6-B0-04-45-C9-B0-04-F8-50-52-C2-17-0C-1D-82-07
//  This is an independent project of an individual developer. Dear PVS-Studio, please check it.
// 
//  PVS-Studio Static Code Analyzer for C, C++, C#, and Java: https://pvs-studio.com
// 
//  Copyright ©  2022 Aleksey Kalduzov. All rights reserved
// 
//  Author: Aleksey Kalduzov
//  Email: alexei.kalduzov@gmail.com
// 
//  Licensed under the Apache License, Version 2.0 (the "License");
//  you may not use this file except in compliance with the License.
//  You may obtain a copy of the License at
// 
//      https://www.apache.org/licenses/LICENSE-2.0
// 
//  Unless required by applicable law or agreed to in writing, software
//  distributed under the License is distributed on an "AS IS" BASIS,
//  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
//  See the License for the specific language governing permissions and
//  limitations under the License.
//
// <auto-generated> THIS CODE IS AUTOMATICALLY GENERATED.  DO NOT EDIT. </auto-generated>

// ReSharper disable RedundantUsingDirective
// ReSharper disable UnusedType.Global
// ReSharper disable UnusedMember.Global
// ReSharper disable ClassNeverInstantiated.Global
// ReSharper disable PartialTypeWithSinglePart

#nullable enable

using NKafka.Exceptions;
using NKafka.Protocol;
using NKafka.Protocol.Buffers;
using NKafka.Protocol.Extensions;
using NKafka.Protocol.Records;
using System.Text;

namespace NKafka.Messages;

/// <summary>
/// Describes the contract for message ConsumerGroupHeartbeatRequestMessage
/// </summary>
internal sealed partial class ConsumerGroupHeartbeatRequestMessage: IRequestMessage, IEquatable<ConsumerGroupHeartbeatRequestMessage>
{
    /// <inheritdoc />
    public ApiKeys ApiKey => ApiKeys.ConsumerGroupHeartbeat;

    /// <summary>
    /// Indicates whether the request is accessed by any broker or only by the controller
    /// </summary>
    public const bool ONLY_CONTROLLER = false;

    /// <inheritdoc />
    public bool OnlyController => ONLY_CONTROLLER;

    /// <inheritdoc />
    public List<TaggedField>? UnknownTaggedFields { get; set; } = null;

    /// <inheritdoc />
    public int IncomingBufferLength { get; private set; } = 0;

    /// <summary>
    /// The group identifier.
    /// </summary>
    public string GroupId { get; set; } = string.Empty;

    /// <summary>
    /// The member id generated by the consumer. The member id must be kept during the entire lifetime of the consumer process.
    /// </summary>
    public string MemberId { get; set; } = string.Empty;

    /// <summary>
    /// The current member epoch; 0 to join the group; -1 to leave the group; -2 to indicate that the static member will rejoin.
    /// </summary>
    public int MemberEpoch { get; set; } = 0;

    /// <summary>
    /// null if not provided or if it didn&#39;t change since the last heartbeat; the instance Id otherwise.
    /// </summary>
    public string? InstanceId { get; set; } = null;

    /// <summary>
    /// null if not provided or if it didn&#39;t change since the last heartbeat; the rack ID of consumer otherwise.
    /// </summary>
    public string? RackId { get; set; } = null;

    /// <summary>
    /// -1 if it didn&#39;t change since the last heartbeat; the maximum time in milliseconds that the coordinator will wait on the member to revoke its partitions otherwise.
    /// </summary>
    public int RebalanceTimeoutMs { get; set; } = -1;

    /// <summary>
    /// null if it didn&#39;t change since the last heartbeat; the subscribed topic names otherwise.
    /// </summary>
    public List<string>? SubscribedTopicNames { get; set; } = null;

    /// <summary>
    /// null if it didn&#39;t change since the last heartbeat; the subscribed topic regex otherwise.
    /// </summary>
    public string? SubscribedTopicRegex { get; set; } = null;

    /// <summary>
    /// null if not used or if it didn&#39;t change since the last heartbeat; the server side assignor to use otherwise.
    /// </summary>
    public string? ServerAssignor { get; set; } = null;

    /// <summary>
    /// null if it didn&#39;t change since the last heartbeat; the partitions owned by the member.
    /// </summary>
    public List<TopicPartitionsMessage>? TopicPartitions { get; set; } = null;

    /// <summary>
    /// The basic constructor of the message ConsumerGroupHeartbeatRequestMessage
    /// </summary>
    public ConsumerGroupHeartbeatRequestMessage()
    {
    }

    /// <summary>
    /// Base constructor for deserializing message ConsumerGroupHeartbeatRequestMessage
    /// </summary>
    public ConsumerGroupHeartbeatRequestMessage(ref BufferReader reader, ApiVersion version)
        : this()
    {
        IncomingBufferLength = reader.Length;
        Read(ref reader, version);
    }

    /// <inheritdoc />
    public void Read(ref BufferReader reader, ApiVersion version)
    {
        {
            int length;
            length = reader.ReadVarInt32() - 1;
            if (length < 0)
            {
                throw new Exception("non-nullable field GroupId was serialized as null");
            }
            else if (length > 0x7fff)
            {
                throw new Exception($"string field GroupId had invalid length {length}");
            }
            else
            {
                GroupId = reader.ReadString(length);
            }
        }
        {
            int length;
            length = reader.ReadVarInt32() - 1;
            if (length < 0)
            {
                throw new Exception("non-nullable field MemberId was serialized as null");
            }
            else if (length > 0x7fff)
            {
                throw new Exception($"string field MemberId had invalid length {length}");
            }
            else
            {
                MemberId = reader.ReadString(length);
            }
        }
        MemberEpoch = reader.ReadInt();
        {
            int length;
            length = reader.ReadVarInt32() - 1;
            if (length < 0)
            {
                InstanceId = null;
            }
            else if (length > 0x7fff)
            {
                throw new Exception($"string field InstanceId had invalid length {length}");
            }
            else
            {
                InstanceId = reader.ReadString(length);
            }
        }
        {
            int length;
            length = reader.ReadVarInt32() - 1;
            if (length < 0)
            {
                RackId = null;
            }
            else if (length > 0x7fff)
            {
                throw new Exception($"string field RackId had invalid length {length}");
            }
            else
            {
                RackId = reader.ReadString(length);
            }
        }
        RebalanceTimeoutMs = reader.ReadInt();
        {
            int arrayLength;
            arrayLength = reader.ReadVarInt32() - 1;
            if (arrayLength < 0)
            {
                SubscribedTopicNames = null;
            }
            else
            {
                var newCollection = new List<string>(arrayLength);
                for (var i = 0; i < arrayLength; i++)
                {
                    int length;
                    length = reader.ReadVarInt32() - 1;
                    if (length < 0)
                    {
                        throw new Exception("non-nullable field SubscribedTopicNames element was serialized as null");
                    }
                    else if (length > 0x7fff)
                    {
                        throw new Exception($"string field SubscribedTopicNames element had invalid length {length}");
                    }
                    else
                    {
                        newCollection.Add(reader.ReadString(length));
                    }
                }
                SubscribedTopicNames = newCollection;
            }
        }
        if (version >= ApiVersion.Version1)
        {
            int length;
            length = reader.ReadVarInt32() - 1;
            if (length < 0)
            {
                SubscribedTopicRegex = null;
            }
            else if (length > 0x7fff)
            {
                throw new Exception($"string field SubscribedTopicRegex had invalid length {length}");
            }
            else
            {
                SubscribedTopicRegex = reader.ReadString(length);
            }
        }
        else
        {
            SubscribedTopicRegex = null;
        }
        {
            int length;
            length = reader.ReadVarInt32() - 1;
            if (length < 0)
            {
                ServerAssignor = null;
            }
            else if (length > 0x7fff)
            {
                throw new Exception($"string field ServerAssignor had invalid length {length}");
            }
            else
            {
                ServerAssignor = reader.ReadString(length);
            }
        }
        {
            int arrayLength;
            arrayLength = reader.ReadVarInt32() - 1;
            if (arrayLength < 0)
            {
                TopicPartitions = null;
            }
            else
            {
                var newCollection = new List<TopicPartitionsMessage>(arrayLength);
                for (var i = 0; i < arrayLength; i++)
                {
                    newCollection.Add(new TopicPartitionsMessage(ref reader, version));
                }
                TopicPartitions = newCollection;
            }
        }
        UnknownTaggedFields = null;
        var numTaggedFields = reader.ReadVarInt32();
        for (var t = 0; t < numTaggedFields; t++)
        {
            var tag = reader.ReadVarInt32();
            var size = reader.ReadVarInt32();
            switch (tag)
            {
                default:
                    UnknownTaggedFields = reader.ReadUnknownTaggedField(UnknownTaggedFields, tag, size);
                    break;
            }
        }
    }

    /// <inheritdoc />
    public void Write(ref BufferWriter writer, ApiVersion version)
    {
        var numTaggedFields = 0;
        {
            var stringBytes = Encoding.UTF8.GetBytes(GroupId);
            writer.WriteVarInt32(stringBytes.Length + 1);
            writer.WriteBytes(stringBytes);
        }
        {
            var stringBytes = Encoding.UTF8.GetBytes(MemberId);
            writer.WriteVarInt32(stringBytes.Length + 1);
            writer.WriteBytes(stringBytes);
        }
        writer.WriteInt(MemberEpoch);
        if (InstanceId is null)
        {
            writer.WriteVarInt32(0);
        }
        else
        {
            var stringBytes = Encoding.UTF8.GetBytes(InstanceId);
            writer.WriteVarInt32(stringBytes.Length + 1);
            writer.WriteBytes(stringBytes);
        }
        if (RackId is null)
        {
            writer.WriteVarInt32(0);
        }
        else
        {
            var stringBytes = Encoding.UTF8.GetBytes(RackId);
            writer.WriteVarInt32(stringBytes.Length + 1);
            writer.WriteBytes(stringBytes);
        }
        writer.WriteInt(RebalanceTimeoutMs);
        if (SubscribedTopicNames is null)
        {
            writer.WriteVarInt32(0);
        }
        else
        {
            writer.WriteVarInt32(SubscribedTopicNames.Count + 1);
            foreach (var element in SubscribedTopicNames)
            {
                {
                    var stringBytes = Encoding.UTF8.GetBytes(element);
                    writer.WriteVarInt32(stringBytes.Length + 1);
                    writer.WriteBytes(stringBytes);
                }
            }
        }
        if (version >= ApiVersion.Version1)
        {
            if (SubscribedTopicRegex is null)
            {
                writer.WriteVarInt32(0);
            }
            else
            {
                var stringBytes = Encoding.UTF8.GetBytes(SubscribedTopicRegex);
                writer.WriteVarInt32(stringBytes.Length + 1);
                writer.WriteBytes(stringBytes);
            }
        }
        else
        {
            if (SubscribedTopicRegex is not null)
            {
                throw new UnsupportedVersionException($"Attempted to write a non-default SubscribedTopicRegex at version {version}");
            }
        }
        if (ServerAssignor is null)
        {
            writer.WriteVarInt32(0);
        }
        else
        {
            var stringBytes = Encoding.UTF8.GetBytes(ServerAssignor);
            writer.WriteVarInt32(stringBytes.Length + 1);
            writer.WriteBytes(stringBytes);
        }
        if (TopicPartitions is null)
        {
            writer.WriteVarInt32(0);
        }
        else
        {
            writer.WriteVarInt32(TopicPartitions.Count + 1);
            foreach (var element in TopicPartitions)
            {
                element?.Write(ref writer, version);
            }
        }
        var rawWriter = RawTaggedFieldWriter.ForFields(UnknownTaggedFields);
        numTaggedFields += rawWriter.FieldsCount;
        writer.WriteVarInt32(numTaggedFields);
        rawWriter.WriteRawTags(ref writer, int.MaxValue);
    }

    /// <inheritdoc />
    public override bool Equals(object? obj)
    {
        return ReferenceEquals(this, obj) || obj is ConsumerGroupHeartbeatRequestMessage other && Equals(other);
    }

    /// <inheritdoc />
    public bool Equals(ConsumerGroupHeartbeatRequestMessage? other)
    {
        if (other is null)
        {
            return false;
        }
        if (GroupId is null)
        {
            if (other.GroupId is not null)
            {
                return false;
            }
        }
        else
        {
            if (!GroupId.Equals(other.GroupId))
            {
                return false;
            }
        }
        if (MemberId is null)
        {
            if (other.MemberId is not null)
            {
                return false;
            }
        }
        else
        {
            if (!MemberId.Equals(other.MemberId))
            {
                return false;
            }
        }
        if (MemberEpoch != other.MemberEpoch)
        {
            return false;
        }
        if (InstanceId is null)
        {
            if (other.InstanceId is not null)
            {
                return false;
            }
        }
        else
        {
            if (!InstanceId.Equals(other.InstanceId))
            {
                return false;
            }
        }
        if (RackId is null)
        {
            if (other.RackId is not null)
            {
                return false;
            }
        }
        else
        {
            if (!RackId.Equals(other.RackId))
            {
                return false;
            }
        }
        if (RebalanceTimeoutMs != other.RebalanceTimeoutMs)
        {
            return false;
        }
        if (SubscribedTopicNames is null)
        {
            if (other.SubscribedTopicNames is not null)
            {
                return false;
            }
        }
        else
        {
            if (!SubscribedTopicNames.SequenceEqual(other.SubscribedTopicNames))
            {
                return false;
            }
        }
        if (SubscribedTopicRegex is null)
        {
            if (other.SubscribedTopicRegex is not null)
            {
                return false;
            }
        }
        else
        {
            if (!SubscribedTopicRegex.Equals(other.SubscribedTopicRegex))
            {
                return false;
            }
        }
        if (ServerAssignor is null)
        {
            if (other.ServerAssignor is not null)
            {
                return false;
            }
        }
        else
        {
            if (!ServerAssignor.Equals(other.ServerAssignor))
            {
                return false;
            }
        }
        if (TopicPartitions is null)
        {
            if (other.TopicPartitions is not null)
            {
                return false;
            }
        }
        else
        {
            if (!TopicPartitions.SequenceEqual(other.TopicPartitions))
            {
                return false;
            }
        }
        return UnknownTaggedFields.CompareRawTaggedFields(other.UnknownTaggedFields);
    }

    /// <inheritdoc />
    public override int GetHashCode()
    {
        var hashCode = 0;
        hashCode = HashCode.Combine(hashCode, GroupId, MemberId, MemberEpoch, InstanceId, RackId, RebalanceTimeoutMs, SubscribedTopicNames);
        hashCode = HashCode.Combine(hashCode, SubscribedTopicRegex, ServerAssignor, TopicPartitions);
        return hashCode;
    }

    /// <inheritdoc />
    public override string ToString()
    {
        return "ConsumerGroupHeartbeatRequestMessage("
            + "GroupId=" + (string.IsNullOrWhiteSpace(GroupId) ? "null" : GroupId)
            + ", MemberId=" + (string.IsNullOrWhiteSpace(MemberId) ? "null" : MemberId)
            + ", MemberEpoch=" + MemberEpoch
            + ", InstanceId=" + (string.IsNullOrWhiteSpace(InstanceId) ? "null" : InstanceId)
            + ", RackId=" + (string.IsNullOrWhiteSpace(RackId) ? "null" : RackId)
            + ", RebalanceTimeoutMs=" + RebalanceTimeoutMs
            + ", SubscribedTopicNames=" + (SubscribedTopicNames is null ? "null" : SubscribedTopicNames.DeepToString())
            + ", SubscribedTopicRegex=" + (string.IsNullOrWhiteSpace(SubscribedTopicRegex) ? "null" : SubscribedTopicRegex)
            + ", ServerAssignor=" + (string.IsNullOrWhiteSpace(ServerAssignor) ? "null" : ServerAssignor)
            + ", TopicPartitions=" + (TopicPartitions is null ? "null" : TopicPartitions.DeepToString())
            + ")";
    }

    /// <summary>
    /// Describes the contract for message TopicPartitionsMessage
    /// </summary>
    internal sealed partial class TopicPartitionsMessage: IMessage, IEquatable<TopicPartitionsMessage>
    {
        /// <inheritdoc />
        public List<TaggedField>? UnknownTaggedFields { get; set; } = null;

        /// <inheritdoc />
        public int IncomingBufferLength { get; private set; } = 0;

        /// <summary>
        /// The topic ID.
        /// </summary>
        public Guid TopicId { get; set; } = Guid.Empty;

        /// <summary>
        /// The partitions.
        /// </summary>
        public List<int> Partitions { get; set; } = new ();

        /// <summary>
        /// The basic constructor of the message TopicPartitionsMessage
        /// </summary>
        public TopicPartitionsMessage()
        {
        }

        /// <summary>
        /// Base constructor for deserializing message TopicPartitionsMessage
        /// </summary>
        public TopicPartitionsMessage(ref BufferReader reader, ApiVersion version)
            : this()
        {
            IncomingBufferLength = reader.Length;
            Read(ref reader, version);
        }

        /// <inheritdoc />
        public void Read(ref BufferReader reader, ApiVersion version)
        {
            if (version > ApiVersion.Version1)
            {
                throw new UnsupportedVersionException($"Can't read version {version} of TopicPartitionsMessage");
            }
            TopicId = reader.ReadGuid();
            {
                int arrayLength;
                arrayLength = reader.ReadVarInt32() - 1;
                if (arrayLength < 0)
                {
                    throw new Exception("non-nullable field Partitions was serialized as null");
                }
                else
                {
                    var newCollection = new List<int>(arrayLength);
                    for (var i = 0; i < arrayLength; i++)
                    {
                        newCollection.Add(reader.ReadInt());
                    }
                    Partitions = newCollection;
                }
            }
            UnknownTaggedFields = null;
            var numTaggedFields = reader.ReadVarInt32();
            for (var t = 0; t < numTaggedFields; t++)
            {
                var tag = reader.ReadVarInt32();
                var size = reader.ReadVarInt32();
                switch (tag)
                {
                    default:
                        UnknownTaggedFields = reader.ReadUnknownTaggedField(UnknownTaggedFields, tag, size);
                        break;
                }
            }
        }

        /// <inheritdoc />
        public void Write(ref BufferWriter writer, ApiVersion version)
        {
            var numTaggedFields = 0;
            writer.WriteGuid(TopicId);
            writer.WriteVarInt32(Partitions.Count + 1);
            foreach (var element in Partitions)
            {
                writer.WriteInt(element);
            }
            var rawWriter = RawTaggedFieldWriter.ForFields(UnknownTaggedFields);
            numTaggedFields += rawWriter.FieldsCount;
            writer.WriteVarInt32(numTaggedFields);
            rawWriter.WriteRawTags(ref writer, int.MaxValue);
        }

        /// <inheritdoc />
        public override bool Equals(object? obj)
        {
            return ReferenceEquals(this, obj) || obj is TopicPartitionsMessage other && Equals(other);
        }

        /// <inheritdoc />
        public bool Equals(TopicPartitionsMessage? other)
        {
            if (other is null)
            {
                return false;
            }
            if (!TopicId.Equals(other.TopicId))
            {
                return false;
            }
            if (Partitions is null)
            {
                if (other.Partitions is not null)
                {
                    return false;
                }
            }
            else
            {
                if (!Partitions.SequenceEqual(other.Partitions))
                {
                    return false;
                }
            }
            return UnknownTaggedFields.CompareRawTaggedFields(other.UnknownTaggedFields);
        }

        /// <inheritdoc />
        public override int GetHashCode()
        {
            var hashCode = 0;
            hashCode = HashCode.Combine(hashCode, TopicId, Partitions);
            return hashCode;
        }

        /// <inheritdoc />
        public override string ToString()
        {
            return "TopicPartitionsMessage("
                + "TopicId=" + TopicId
                + ", Partitions=" + Partitions.DeepToString()
                + ")";
        }
    }
}
